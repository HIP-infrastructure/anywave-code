function [MEG_PNG, EEG_PNG] = aw_captureicapanel()
% aw_captureicapanel request a capture of the current MEG and EEG ICA
% components panels open in AnyWave
% if the panels are not open in AnyWave, returns empty strings
% otherwise returns the path the captured png files.

end