function aw_setbf(type, anywave_labels, source_labels, matrix, lowpass, highpass )
%aw_setbf send a beamformer transfert matrix to AnyWave
%   type = type of sources to add ( 'EEG' or 'MEG' ) 
%   anywave_labels = labels of channels in AnyWave use to compute the
%   sources channels (cell array of strings).
%   source_labels = labels of future source channels. (cell array of
%   strings)
%   matrix = transfer matrix.
%   lowpass = the low pass filter frequency used to compute the transfer matrix.
%   highpass = the high pass filter frequency used to compute the transfer matrix.
%
end

